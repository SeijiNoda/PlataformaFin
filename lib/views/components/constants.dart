import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const primaryColor = Colors.blue;
  static const secondaryColor = Colors.blue;

  // Cores de texto
  static const textColor = Colors.black;
  static const textSecondaryColor = Colors.white;  // Cor para o texto branco

  // Cores de fundo
  static const backgroundColor = Colors.white;
  static const scaffoldBackgroundColor = Colors.grey;

  // Cores de borda
  static const borderColor = Colors.black;

  // Cores de alerta
  static const successColor = Colors.greenAccent;
  static const errorColor = Colors.redAccent;
}
